import HeroContent from "./HeroContent";

const Hero = () => (
    <div className="hero container">
        <div className="row">
            <div className="col">
                <HeroContent />
            </div>
        </div>
    </div>
);

export default Hero;