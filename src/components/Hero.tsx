import HeroContent from "./HeroContent";

const Hero = () => (
    <div className="hero container">
                <HeroContent />
        <div className="row">
            <div className="col">
            </div>
        </div>
    </div>
);

export default Hero;